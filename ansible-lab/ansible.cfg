[defaults]

# FOR HOME BRANCH
inventory = home_inventory.yaml

# FOR LAB BRANCH
# inventory = lab_inventory.yml
deprecation_warnings=False

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True

[ssh_connection]
pipelining = True