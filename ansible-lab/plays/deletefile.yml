---
- name: Delete a directory
  hosts: web_servers
  tasks:
    - name: Remove ansible-directory
      ansible.builtin.file:
        path: "{{ ansible_env.HOME }}/ansible-directory"
        state: absent